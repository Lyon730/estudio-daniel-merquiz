rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Reglas para la galería
    match /galeria/{allPaths=**} {
      // Permitir lectura a todos
      allow read: if true;
      
      // Permitir escritura a todos (simplificado)
      allow write: if true;
    }
  }
}
